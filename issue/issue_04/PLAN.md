# issue/04 실행 계획 — 26 이중 층 회수 (crane-eaten 7.4% + nestle-미스 밴드 8.3%)

작성 2026-07-17. 근거: 재프로브 관문 통과(신 기준선 26=10.84M 해에서 crane-eaten 7.41%,
비가시 밴드 8.33% — count p90=24로 **배포 k32 안쪽**), 26 = 용량 하한 0 인증(순수 모양 병인,
전액이 이 미세층 대상), N2 전례(밴드 12.55% → T300 −16%). 새 은행 26 = 11.51M.

## Step 0 — 귀속 실험 (코드 0줄, 기존 knob·30분 규칙)

밴드 8.3%가 배포 엔진에서 새는 원인 4후보를 knob A/B로 분리:
raw decode(결정적) 26 × {base(cap12·flop2e9), cap32, cap96, flop1e12, cap32+flop1e12,
K64+cap32}. obj가 cap/flop 축에서 움직이면 그 knob이 몸통(→ 메커니즘 B), 전부 평평하면
잔여 = gate 거부(→ 메커니즘 A) 또는 프로브 상태-차이(반사실 아티팩트).
⚠️raw는 방향 참고만(pruning 금지 규율) — 채택은 T=300.

## 메커니즘 A — crane 게이트의 체커-기준 미러 재판정 (issue/04 본안)

`_try_nestle`에서 space_ok 통과 후 `_exact_gate`가 거부한 anchor를 **shapely 미러**(변 접촉 =
면적 0 = 합법, EPS 허용)로 1회 재판정. 기하 구조는 ExactSpace가 이미 보유(suffix-union) —
확장: ①내 EXIT 시점 stayers 대상 동일 검사 ②조기-퇴장 상주의 exit sweep(상주 layer k vs 내
layers ≥ k) 역방향 검사. flag `dispatch_nestle_crane_mirror`(False=off=byte 동일), nestle
FLOP 캡 지배下(신규 스캔 0). 신뢰 표면 0(최종 공식 체커 인증 불변 — 미러가 틀리면 repair가
교정, N2와 동일 원칙).

## 메커니즘 B — cap/FLOP 재조정 (Step 0 결과 조건부)

cap 12→{24, 32} 또는 flop_cap 상향(06d count_scan 캐시로 비용 구조가 바뀐 뒤 재보정된 적
없음). 배선 스코프 = dyn-on 2워커 전역이라 회귀 노출 넓음 → 전40 60s 열 A/B + loo60 조인
상한 필수.

## 검증 사다리 (사전등록 — 관례 그대로)

S0 off=byte 동일 + 결정론 → Step0 귀속 → 빌드(A 그리고/또는 B) → **T=300 부하-쌍대
{26} × {κ3-fd, κ1} × seed {1,5} paired** ≤−3% & 부호 일관 → 3seed 승격 → 캐너리 T=300
{39, 30}(nestle 최대 수혜군 = 노출 집합) → 전40 60s 열 A/B + loo60 조인 → 크래시 프록시
3종(배선 직전) → 기전 확인 = **diag_nestle 재프로브**(crane-eaten·밴드 실제 감소 확인) →
배선(pytest·스모크 포함).

## Kill·승계 (사전등록)

- Step 0 전부 평평 + A(T300 26 2cfg 전패) → 사인 기록("프로브 상태-차이 = 반사실 아티팩트"
  vs "미러 무효"를 재프로브로 구분) → **코드 제거 우선** → 승계 = ⓐ 38 bay0 미활용 진단
  (측정-선행: 왜 비는지 — eligibility/라우팅/배정 분해)으로 화력 전환.
- B만 승리 시 A 제거, A만 승리 시 B 기본값 유지.
- 예산-민감 가드: T300 승 + 60s 기각 = "채점 T≥300 확인 시 배선" 보관(즉사 아님).

## 기대 크기 (정직)

26 −5~10%(−0.6~1.2M) +  crane-eaten 2~3% 층의 타 문제 spillover. N2 전례(12.5%→−16%)의
절반 규모 밴드(8.3%+7.4% 중 실현분).

---

## Results — 메커니즘 B(cap/FLOP 재보정) 승격 완료 (2026-07-17, 원시 데이터 = reports/nestle_recal/)

**Step 0 귀속(코드 0줄)**: 26 raw에서 cap 12→32 = −6.3%·FLOP 해제 = −7.3%·둘 다 = **−12.5% +
디코드 2.8배 가속**(제대로 앉히면 하류 막힘 감소). K64는 해(−10.3%) = K는 32로 충분.
06d 가속 후 예산 미재보정이 병인이라는 가설 그대로.

**사다리 결과(전부 T=300 부하-쌍대 paired, B20 = cap32·flop2e10)**:
- 26: κ3-fd 3seed −5.1/−8.2/−8.9% · κ1 3seed −12.6/−7.9/−9.5% = **6/6 부호 일관**.
- 캐너리: 30 −12.0% · 39 −6.8% (전승 — 회수 수혜군 전체가 옛 예산에 졸려 있었음).
- B20 == B∞ 동일값 = 2e10이 안 마름 = P6-안전 유계 선택.
- 표적 게이트(전40 대신 **핵심12 + 발화-스크린 28**, 사용자 지적로 재설계 — 전40 폐기):
  핵심12 열총합 κ3 −7.2%·κ1 −6.2%, 미발화 22/28 증명. loo60 조인 포트폴리오-상한 순 **−10.41M**
  (회귀 {27 +459k, 32 +525k}).
- T=300 스폿: **27 반전 확인**(κ3 −2.7%·κ1 −4.2%, 새 최저 26.09M) · **32 지속 회귀 +9.4%**.
- 배선안 비교(조인 산법): 양열 −10.41M > κ3만 −9.92M >> κ1만 −2.16M → **양열 배선**(32 노출
  수용, 상한 +0.3~0.5M = 총이득 1/20).

**배선·검증**: portfolio nes = k32/cap32/flop2e10(dyn-on 2워커). pytest 31/31 · 크래시 프록시
3종 within-T(107~108s) · 배포 스모크 26 T300 = 10.50M · **32 노출 실현 +0.3M**(3.32M vs κ3
단독 3.02M — 예상 상한 내, 기록 후 수용).

**기전 확인(diag_nestle 재프로브, B20 26 해 = 10.28M)**: **비가시 밴드 8.33% → 1.99%**,
crane-eaten 7.41 → 5.97%. 잔여 밴드 count p50/p90 = 33/67 = **K=32 절단 바깥만 잔존**(cap
회수 완료의 직접 증거). → issue/04의 잔여 = crane-eaten ~6%(메커니즘 A, 별도 판단) + K-절단
바깥 소량.

**방법론(사용자 지적로 확립)**: 발화 조건이 좁은 레버(mask 전멸 시에만 등)의 배선 게이트는
전40이 아니라 **발화 집합 실측 + 나머지 raw 발화-스크린** — 완전동일값이 무접촉 증명.
